# Posture Correctness — Progress (mutable loop state)

**Single source of truth for "what's done" = this file + the `plan.md` checklist
+ `git log`.** Cold-start iterations read this first.

## Current Step
Step 0 — not started.

## Type Map
*(Populate in Step 0 — verified names/fields, do not assume.)*
- `PostureVisualizationViewModel` calibration-capture region: _TBD (grep ~lines 125–138)_
- State read (`isCalibrating` / `PostureState` cases): _TBD_
- Current head-yaw source field: _TBD (expected `PoseSample.shoulderTwist`)_
- Nose/ear keypoints exposed to ViewModel? _TBD_ — if not, substitution: _TBD_
- Working simulator destination: _TBD_

## Verification Notes
*(Append one per completed step: tests run, build result, commit hash,
decisions, any regressions, substitutions made.)*

## Known blockers
*(none yet)*
